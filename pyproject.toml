[project]
name = "taskscheduling-env"
version = "0.1.0"
description = "Task Scheduling OpenEnv Project"
authors = [{name = "Neha"}]
dependencies = [
    "fastapi",
    "uvicorn",
    "openenv>=0.2.0"
]

[tool.openenv]
entrypoint = "server.app:app"
